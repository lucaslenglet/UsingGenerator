# Whitespace and encoding only. Style rules are deliberately left out: the repo builds with
# TreatWarningsAsErrors, so adding IDE severities here would turn opinions into build failures.
root = true

[*]
charset = utf-8
end_of_line = crlf
indent_style = space
indent_size = 4
insert_final_newline = true
trim_trailing_whitespace = true

[*.{json,yml,yaml}]
indent_size = 2

[*.{csproj,props,targets,slnx}]
indent_size = 2

[*.md]
trim_trailing_whitespace = false
